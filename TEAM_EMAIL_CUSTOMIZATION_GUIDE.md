# 📧 دليل تخصيص إيميلات الفريق

## نظرة عامة
تم إضافة ميزة تخصيص محتوى إيميلات الفريق قبل إرسالها، مع إمكانية إضافة رابط كتيب PDF ومرفقات إضافية.

---

## 🎯 الميزات الجديدة

### 1. مودال تخصيص الإيميل
عند الضغط على زر "إرسال إيميلات" بجانب أي فريق، يظهر مودال يحتوي على:

#### أ. عنوان الإيميل
- **الحقل**: `customSubject`
- **القيمة الافتراضية**: `📋 تفاصيل فريقك - {اسم الفريق}`
- **الوصف**: عنوان الإيميل الذي سيراه المستلمون

#### ب. الرسالة الرئيسية
- **الحقل**: `customMessage`
- **القيمة الافتراضية**: رسالة ترحيبية بسيطة
- **الوصف**: محتوى الرسالة الذي سيظهر في بداية الإيميل
- **مثال**: "إليك المعلومات والإرشادات الخاصة بفريقك في الهاكاثون. نتمنى لكم التوفيق والنجاح!"

#### ج. رابط الكتيب الإرشادي (PDF)
- **الحقل**: `pdfLink`
- **اختياري**: نعم
- **نوع الحقل**: URL
- **الوصف**: رابط لملف PDF يحتوي على دليل الهاكاثون
- **مثال**: `https://example.com/hackathon-guide.pdf`
- **التأثير**: يظهر قسم منفصل في الإيميل مع زر تحميل الكتيب

#### د. ملاحظات إضافية
- **الحقل**: `additionalNotes`
- **اختياري**: نعم
- **نوع الحقل**: نص متعدد الأسطر
- **الوصف**: ملاحظات أو تعليمات خاصة للفريق
- **التأثير**: يظهر في قسم "💡 ملاحظات إضافية" في الإيميل

---

## 🖼️ معاينة الإيميل

### البنية الأساسية للإيميل:

```
┌─────────────────────────────────┐
│  📋 تفاصيل فريقك                │  ← Header
├─────────────────────────────────┤
│                                 │
│  مرحباً {اسم المستخدم}! 👋      │
│                                 │
│  {الرسالة المخصصة}              │  ← Custom Message
│                                 │
│  ┌──────────────────────────┐  │
│  │ 🏆 {اسم الفريق}          │  │
│  │ دورك: {الدور المفضل}     │  │
│  └──────────────────────────┘  │
│                                 │
│  👥 أعضاء فريقك:                │
│  ┌──────────────────────────┐  │
│  │ عضو 1 (دوره)             │  │
│  │ عضو 2 (دوره)             │  │
│  │ عضو 3 (دوره)             │  │
│  └──────────────────────────┘  │
│                                 │
│  📚 الكتيب الإرشادي             │  ← إذا تم إضافة PDF Link
│  ┌──────────────────────────┐  │
│  │ 📥 تحميل الكتيب (PDF)    │  │  ← زر قابل للضغط
│  └──────────────────────────┘  │
│                                 │
│  💡 ملاحظات إضافية:             │  ← إذا تم إضافة ملاحظات
│  {النص الإضافي}                 │
│                                 │
│  💡 نصيحة: تواصل مع فريقك!     │
│                                 │
│  🏠 زيارة الملف الشخصي          │  ← زر (admin version)
│                                 │
└─────────────────────────────────┘
```

---

## 🔧 التفاصيل التقنية

### 1. الملفات المعدلة

#### أ. `components/admin/TeamsDisplay.tsx`
**التعديلات**:
- إضافة States جديدة:
  ```typescript
  const [showEmailModal, setShowEmailModal] = useState(false)
  const [selectedTeamForEmail, setSelectedTeamForEmail] = useState<{id: string, name: string} | null>(null)
  const [emailSubject, setEmailSubject] = useState('')
  const [emailMessage, setEmailMessage] = useState('')
  const [pdfLink, setPdfLink] = useState('')
  const [additionalNotes, setAdditionalNotes] = useState('')
  const [sendingEmail, setSendingEmail] = useState(false)
  ```

- دالة `openEmailModal`: تفتح المودال وتضبط القيم الافتراضية
- دالة `sendTeamEmails` المحدثة: ترسل البيانات المخصصة للـ API
- مودال تخصيص الإيميل: Dialog كامل مع جميع الحقول

#### ب. `app/api/admin/hackathons/[id]/teams/[teamId]/send-emails/route.ts`
**التعديلات**:
- قراءة البيانات المخصصة من request body:
  ```typescript
  const { customSubject, customMessage, pdfLink, additionalNotes } = body
  ```

- تمرير البيانات لدالة إنشاء الإيميل:
  ```typescript
  getTeamUpdateEmailContent(
    userName,
    hackathonTitle,
    teamName,
    userRole,
    teamMembersList,
    customMessage,
    pdfLink,
    additionalNotes
  )
  ```

- تحديث دالة `getTeamUpdateEmailContent` لتقبل المعاملات الإضافية

#### ج. `app/api/supervisor/hackathons/[id]/teams/[teamId]/send-emails/route.ts`
**التعديلات**: نفس التعديلات المذكورة في ملف admin

---

## 📝 أمثلة الاستخدام

### مثال 1: إرسال إيميل بكتيب PDF

**السيناريو**: تريد إرسال إيميل للفريق مع كتيب إرشادات الهاكاثون

**الخطوات**:
1. اضغط على زر "إرسال إيميلات" بجانب الفريق
2. في المودال، أدخل:
   - **العنوان**: `📋 معلومات مهمة عن فريقك`
   - **الرسالة**: `مرحباً بكم في الهاكاثون! نرجو الاطلاع على الكتيب الإرشادي المرفق`
   - **رابط PDF**: `https://drive.google.com/file/d/xxx/view?usp=sharing`
3. اضغط "إرسال الإيميلات"

**النتيجة**: 
- سيصل إيميل لكل عضو في الفريق
- يحتوي على رسالتك المخصصة
- يحتوي على زر تحميل الكتيب

---

### مثال 2: إضافة ملاحظات مهمة

**السيناريو**: تريد تنبيه الفريق لموعد مهم

**الخطوات**:
1. افتح مودال الإيميل
2. أدخل في "ملاحظات إضافية":
   ```
   ⏰ تنبيه مهم:
   - الموعد النهائي لتسليم المشروع: 25 أكتوبر
   - اجتماع تقييم أولي: 20 أكتوبر الساعة 10 صباحاً
   - يرجى التواصل مع المشرف قبل البدء بالتطوير
   ```
3. أرسل

**النتيجة**:
- الإيميل سيحتوي على قسم "💡 ملاحظات إضافية" باللون الأزرق
- الملاحظات تظهر بوضوح مع الحفاظ على التنسيق

---

## 🎨 التصميم

### ألوان النظام
- **Header**: Gradient من `#01645e` إلى `#3ab666`
- **قسم PDF**: خلفية صفراء `#fff3cd` مع حدود `#ffc107`
- **قسم الملاحظات**: خلفية زرقاء فاتحة `#e3f2fd` مع حدود `#2196f3`
- **النصائح**: خلفية `#fff8e1` مع حدود `#c3e956`

### المساحات
- **Modal Width**: `700px`
- **Max Height**: `90vh` (قابل للتمرير)
- **Padding**: متناسق في جميع الأقسام

---

## ✅ التحقق من الصحة

### في الـ Frontend:
- عنوان الإيميل مطلوب (`required`)
- الرسالة الرئيسية مطلوبة (`required`)
- رابط PDF يجب أن يكون URL صحيح (اختياري)
- زر الإرسال معطل إذا كانت الحقول المطلوبة فارغة

### في الـ Backend:
- التحقق من وجود الفريق
- التحقق من وجود أعضاء في الفريق
- معالجة القيم الاختيارية (undefined إذا لم تُرسل)

---

## 🔒 الصلاحيات

### Admin:
- ✅ الوصول الكامل لجميع الفرق
- ✅ إرسال إيميلات بدون قيود

### Supervisor:
- ✅ فقط للهاكاثونات المعينة له
- ✅ يجب أن تكون صلاحية `canSendMessages` مفعلة
- ❌ لا يمكن الوصول لفرق هاكاثونات أخرى

---

## 📊 معلومات الإرسال

### ما يتم إرساله تلقائياً:
- اسم المستلم
- اسم الفريق
- دور المستلم في الفريق
- قائمة بجميع أعضاء الفريق وأدوارهم
- نصائح للعمل الجماعي
- رابط الملف الشخصي (admin version)

### ما يمكن تخصيصه:
- ✏️ عنوان الإيميل
- ✏️ الرسالة الرئيسية
- ✏️ رابط الكتيب (اختياري)
- ✏️ ملاحظات إضافية (اختياري)

---

## 🐛 استكشاف الأخطاء

### المشكلة: الإيميلات لا تصل
**الحل**: 
1. تحقق من إعدادات Gmail في `.env`
2. تأكد من أن `GMAIL_USER` و `GMAIL_PASS` صحيحة
3. راجع logs في الـ console

### المشكلة: رابط PDF لا يعمل
**الحل**:
1. تأكد أن الرابط يبدأ بـ `http://` أو `https://`
2. تأكد أن الملف متاح للعامة (public)
3. جرب الرابط في المتصفح قبل إرساله

### المشكلة: الملاحظات لا تظهر بالتنسيق الصحيح
**الحل**:
- استخدم أسطر فارغة للفصل بين الفقرات
- النصوص المتعددة الأسطر تُحفظ كما هي (`white-space: pre-wrap`)

---

## 🚀 المزايا المستقبلية المقترحة

### قيد التطوير:
- [ ] رفع ملف PDF مباشرة بدلاً من رابط
- [ ] إضافة معاينة للإيميل قبل الإرسال
- [ ] حفظ قوالب إيميل مخصصة
- [ ] إرسال إيميلات لفرق متعددة دفعة واحدة
- [ ] تتبع معدل فتح الإيميلات
- [ ] إضافة مرفقات متعددة

### تحسينات الـ UI:
- [ ] Rich text editor للرسالة
- [ ] معاينة حية للإيميل
- [ ] اختبار إرسال لإيميل واحد
- [ ] تاريخ الإيميلات المرسلة

---

## 📞 الدعم

في حالة وجود مشاكل أو استفسارات:
1. راجع الـ logs في الـ console
2. تحقق من الصلاحيات
3. تأكد من صحة البيانات المدخلة

---

**آخر تحديث**: 19 أكتوبر 2025  
**الإصدار**: 1.0.0  
**Commit**: 5d31f83
