# 🎯 دليل الحقول الشرطية (Conditional Fields)

## 📅 التاريخ: 15 أكتوبر 2025
## 🔄 Commit: 5b3b9fc

---

## ✨ الميزة الجديدة

تم إضافة خاصية **الحقول الشرطية** التي تسمح بإظهار حقول معينة فقط عندما يختار المستخدم قيمة محددة في حقل آخر.

---

## 🎯 الهدف

السماح بإنشاء نماذج ديناميكية ذكية، مثل:
- **الوضع الحالي:** طالب | موظف | أخرى
- إذا اختار **"أخرى"** → يظهر حقل نص لكتابة الوضع

---

## 🔧 كيفية الاستخدام

### الخطوة 1: إنشاء الحقل الأساسي (الشرط)

1. اذهب إلى: `/admin/hackathons/[id]/registration-form`
2. اضغط **"إضافة حقل"**
3. املأ البيانات:
   - **نوع الحقل:** قائمة منسدلة (Select) أو اختيار واحد (Radio)
   - **تسمية الحقل:** الوضع الحالي
   - **الخيارات:**
     - طالب
     - موظف
     - أخرى

### الخطوة 2: إضافة الحقل الشرطي

1. اضغط **"إضافة حقل"** مرة أخرى
2. املأ البيانات:
   - **نوع الحقل:** نص (Text) أو نص طويل (Textarea)
   - **تسمية الحقل:** حدد وضعك الحالي
   - **النص التوضيحي:** اكتب وضعك الحالي بالتفصيل

### الخطوة 3: تفعيل الشرط

في الحقل الجديد:

1. فعّل **"حقل شرطي (يظهر عند شرط معين)"**
2. في **"يظهر عندما"**: اختر **"الوضع الحالي"**
3. في **"يساوي"**: اختر **"أخرى"**
4. اضغط **"حفظ النموذج"**

---

## 🎨 مثال عملي كامل

### السيناريو: نموذج تسجيل هاكاثون

#### الحقول:
```
1. الاسم الكامل (نص - مطلوب)
2. البريد الإلكتروني (بريد - مطلوب)
3. رقم الهاتف (هاتف - مطلوب)
4. الوضع الحالي (قائمة منسدلة - مطلوب)
   - طالب
   - موظف
   - أخرى
5. حدد وضعك الحالي (نص طويل - مطلوب)
   🔹 يظهر عندما: الوضع الحالي = "أخرى"
6. التخصص (قائمة منسدلة - مطلوب)
   - علوم حاسب
   - هندسة برمجيات
   - تصميم
   - أخرى
7. حدد تخصصك (نص - مطلوب)
   🔹 يظهر عندما: التخصص = "أخرى"
```

---

## 🖼️ واجهة المستخدم

### في صفحة Admin (إنشاء النموذج):

```
┌─────────────────────────────────────┐
│  حقل 5: حدد وضعك الحالي            │
├─────────────────────────────────────┤
│  نوع الحقل:        [نص طويل]       │
│  تسمية الحقل:      حدد وضعك...     │
│  النص التوضيحي:    اكتب وضعك...    │
│  ☑ حقل مطلوب                       │
├─────────────────────────────────────┤
│  ☑ حقل شرطي (يظهر عند شرط معين)  │
├─────────────────────────────────────┤
│  يظهر عندما:                        │
│  [الوضع الحالي ▼]                  │
│                                     │
│  يساوي:                             │
│  [أخرى ▼]                           │
│                                     │
│  💡 هذا الحقل سيظهر فقط عندما       │
│     يختار المستخدم "أخرى" في حقل    │
│     "الوضع الحالي"                  │
└─────────────────────────────────────┘
```

### في صفحة التسجيل (للمشاركين):

**قبل اختيار "أخرى":**
```
┌─────────────────────────────────────┐
│  الوضع الحالي *                    │
│  [اختر الوضع الحالي ▼]             │
└─────────────────────────────────────┘
```

**بعد اختيار "أخرى":**
```
┌─────────────────────────────────────┐
│  الوضع الحالي *                    │
│  [أخرى ▼]                           │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  حدد وضعك الحالي *                 │
│  ┌───────────────────────────────┐  │
│  │ اكتب وضعك الحالي بالتفصيل... │  │
│  │                               │  │
│  │                               │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 🔧 التفاصيل التقنية

### 1. Interface Updates

#### في Admin (`registration-form/page.tsx`):
```typescript
interface FormField {
  id: string
  type: 'text' | 'email' | ...
  label: string
  required: boolean
  conditional?: {
    enabled: boolean
    showWhen: string        // ID of the field to watch
    showWhenValue: string   // Value that triggers show
  }
}
```

#### في Public (`register-form/page.tsx`):
```typescript
const shouldShowField = (field: FormField): boolean => {
  if (!field.conditional?.enabled) return true
  
  const watchFieldValue = formData[field.conditional.showWhen]
  return watchFieldValue === field.conditional.showWhenValue
}
```

### 2. UI Components

#### Admin Panel:
```tsx
<Switch
  checked={field.conditional?.enabled || false}
  onCheckedChange={(checked) => updateField(field.id, {
    conditional: {
      enabled: checked,
      showWhen: '',
      showWhenValue: ''
    }
  })}
/>

{field.conditional?.enabled && (
  <Select value={field.conditional?.showWhen}>
    {/* List of select/radio fields */}
  </Select>
)}
```

#### Public Form:
```tsx
{form.fields
  .filter(field => shouldShowField(field))
  .map(field => (
    <motion.div
      initial={{ opacity: 0, height: 0 }}
      animate={{ opacity: 1, height: 'auto' }}
      transition={{ duration: 0.3 }}
    >
      {renderField(field)}
    </motion.div>
  ))
}
```

### 3. Validation

```typescript
// Validate only visible fields
form.fields.forEach(field => {
  if (shouldShowField(field)) {
    const error = validateField(field, formData[field.id])
    if (error) {
      newErrors[field.id] = error
    }
  }
})
```

---

## 🎬 الـ Animation

عند إظهار/إخفاء الحقول:
- ✅ Fade in/out (opacity)
- ✅ Slide down/up (height)
- ✅ Smooth transition (0.3s)

---

## 🚀 أمثلة استخدام متقدمة

### مثال 1: التخصص الدراسي
```
الكلية:
  - هندسة
  - طب
  - علوم
  - أخرى

حدد كليتك: (يظهر عند: الكلية = أخرى)
```

### مثال 2: خبرة البرمجة
```
مستوى الخبرة:
  - مبتدئ
  - متوسط
  - متقدم
  - محترف

عدد سنوات الخبرة: (يظهر عند: مستوى الخبرة = محترف)
```

### مثال 3: الجنسية
```
الجنسية:
  - سعودي
  - مقيم
  - أخرى

حدد جنسيتك: (يظهر عند: الجنسية = أخرى)
رقم الإقامة: (يظهر عند: الجنسية = مقيم)
```

### مثال 4: نوع الفريق
```
نوع المشاركة:
  - فردي
  - فريق
  - أريد الانضمام لفريق

عدد أعضاء الفريق: (يظهر عند: نوع المشاركة = فريق)
مجال الاهتمام: (يظهر عند: نوع المشاركة = أريد الانضمام لفريق)
```

---

## ⚠️ القيود الحالية

### 1. يعمل فقط مع:
- ✅ Select (قائمة منسدلة)
- ✅ Radio (اختيار واحد)

### 2. لا يعمل مع:
- ❌ Checkbox (مربع اختيار متعدد)
- ❌ Text/Email/Phone (حقول إدخال نصية)

### 3. شرط واحد فقط:
- الحقل يمكن أن يعتمد على حقل واحد فقط
- لا يدعم شروط متعددة حالياً (AND/OR)

---

## 🎯 سير العمل

### 1. عند تغيير قيمة الحقل الأساسي:
```
User selects "أخرى"
   ↓
formData['status'] = 'أخرى'
   ↓
shouldShowField() checks condition
   ↓
Returns true for conditional field
   ↓
Field appears with animation
```

### 2. عند إرسال النموذج:
```
Form Submit
   ↓
Filter visible fields
   ↓
Validate only visible fields
   ↓
Send data (including conditional field if shown)
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: الحقل لا يظهر
**الحل:**
1. تأكد أن الحقل الأساسي من نوع Select أو Radio
2. تأكد أن القيمة في "يساوي" مطابقة تماماً للخيار
3. تحقق أن الـ Switch مفعل

### المشكلة: الحقل يظهر دائماً
**الحل:**
1. تأكد أن الـ Switch مفعل
2. تأكد من اختيار الحقل الصحيح في "يظهر عندما"

### المشكلة: خطأ في التحقق (Validation)
**الحل:**
- الحقول الشرطية غير المرئية لا يتم التحقق منها
- فقط الحقول المرئية يتم التحقق منها

---

## 📊 الإحصائيات

```
2 ملفات معدلة
+127 إضافات
-8 حذف

الملفات:
- registration-form/page.tsx (Admin)
- register-form/page.tsx (Public)
```

---

## 🎨 التصميم

### Admin Panel:
- 🔵 خلفية زرقاء فاتحة للحقول الشرطية
- 🔵 Border أزرق
- 💡 رسالة توضيحية باللون الأزرق

### Public Form:
- ✨ Smooth animation عند الظهور/الاختفاء
- 📱 Responsive على جميع الشاشات
- 🎯 نفس التنسيق للحقول العادية

---

## 🚀 التحديثات المستقبلية

### مقترحات للتطوير:

1. **شروط متعددة:**
   ```
   يظهر عندما:
     - الحقل 1 = قيمة 1
     AND
     - الحقل 2 = قيمة 2
   ```

2. **شروط معقدة:**
   ```
   يظهر عندما:
     - (الحقل 1 = قيمة 1 OR الحقل 1 = قيمة 2)
     AND
     - الحقل 2 != قيمة 3
   ```

3. **دعم جميع أنواع الحقول:**
   - Checkbox (عند اختيار خيار معين)
   - Text (عند إدخال نص معين)

4. **Conditional Validation:**
   ```
   البريد الإلكتروني:
     - مطلوب إذا: نوع التواصل = "بريد إلكتروني"
     - اختياري في الحالات الأخرى
   ```

5. **Show/Hide Animation Options:**
   - Slide from left/right
   - Fade only
   - Scale

---

## ✅ الخلاصة

الآن يمكنك إنشاء نماذج ذكية ومرنة:
- ✅ حقول تظهر فقط عند الحاجة
- ✅ تجربة مستخدم أفضل
- ✅ نماذج أقصر وأكثر تركيزاً
- ✅ Validation ذكي للحقول المرئية فقط

**رابط التجربة:**
`/admin/hackathons/[id]/registration-form`

**مثال تطبيقي:**
```
الوضع الحالي:
  - طالب
  - موظف
  - أخرى
    
حدد وضعك: (يظهر عند اختيار "أخرى")
  [نص طويل لكتابة الوضع...]
```

---

**تم بنجاح! الحقول الشرطية جاهزة للاستخدام! 🎉**
