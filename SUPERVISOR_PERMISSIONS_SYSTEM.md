# نظام صلاحيات المشرفين المتقدم

## نظرة عامة
تم تطوير نظام صلاحيات متقدم للمشرفين يمنح الأدمن تحكماً كاملاً في صلاحيات كل مشرف على حدة، مع واجهة سحب وإفلات (Drag & Drop) لنقل الأعضاء بين الفرق.

## الميزات الجديدة

### 1. صلاحيات كاملة للمشرف على الهاكاثون المخصص له
المشرف الآن لديه نفس صلاحيات الأدمن على الهاكاثون المخصص له، بما في ذلك:

#### إدارة المشاركين
- ✅ عرض قائمة المشاركين
- ✅ قبول المشاركين
- ✅ رفض المشاركين
- ✅ تعديل حالة المشاركين

#### إدارة الفرق
- ✅ عرض جميع الفرق
- ✅ نقل الأعضاء بين الفرق عبر **Drag & Drop** (سحب وإفلات)
- ✅ إزالة الأعضاء من الفرق بنقرة واحدة
- ✅ تعديل معلومات الفرق
- ✅ واجهة تفاعلية مع مؤشرات بصرية أثناء السحب

#### التقارير والبيانات
- ✅ عرض التقارير والإحصائيات
- ✅ تصدير البيانات

#### التواصل
- ✅ إرسال الرسائل للمشاركين

### 2. نظام إدارة الصلاحيات من لوحة الأدمن

تم إضافة واجهة متقدمة في `/admin/supervisors-management` تتيح للأدمن:

#### تفعيل/تعطيل المشرف
- يمكن للأدمن تعطيل مشرف معين مؤقتاً دون حذف التعيين
- عند التعطيل، لن يتمكن المشرف من الوصول إلى أي صفحات أو APIs

#### تخصيص الصلاحيات
يمكن للأدمن تخصيص الصلاحيات التالية لكل مشرف:

**إدارة المشاركين:**
- ☑️ عرض وإدارة المشاركين
- ☑️ قبول المشاركين
- ☑️ رفض المشاركين

**إدارة الفرق:**
- ☑️ عرض وإدارة الفرق
- ☑️ نقل الأعضاء بين الفرق
- ☑️ إزالة الأعضاء من الفرق

**التقارير والبيانات:**
- ☑️ عرض التقارير
- ☑️ تصدير البيانات

**التواصل:**
- ☑️ إرسال الرسائل للمشاركين

## API Endpoints الجديدة

### 1. تحديث صلاحيات المشرف
```
PATCH /api/admin/supervisor-assignments/[id]/permissions
```

**Body:**
```json
{
  "permissions": {
    "canManageParticipants": true,
    "canApproveParticipants": true,
    "canRejectParticipants": true,
    "canManageTeams": true,
    "canMoveMembers": true,
    "canRemoveMembers": true,
    "canViewReports": true,
    "canExportData": true,
    "canSendMessages": true
  }
}
```

### 2. تفعيل/تعطيل المشرف
```
PATCH /api/admin/supervisor-assignments/[id]/status
```

**Body:**
```json
{
  "isActive": false
}
```

### 3. نقل عضو بين الفرق (للمشرف) - يدعم Drag & Drop
```
POST /api/supervisor/hackathons/[id]/teams/[teamId]/members/[participantId]/move
```

**الاستخدام:**
- يتم استدعاء هذا الـ API تلقائياً عند سحب وإفلات عضو من فريق لآخر
- يمكن أيضاً استخدامه مباشرة عبر POST request

**Body:**
```json
{
  "targetTeamId": "team_id_here"
}
```

### 4. إزالة عضو من فريق (للمشرف)
```
DELETE /api/supervisor/hackathons/[id]/teams/[teamId]/members/[participantId]
```

## كيفية الاستخدام

### للأدمن:

1. **الوصول إلى صفحة إدارة المشرفين:**
   ```
   /admin/supervisors-management
   ```

2. **تخصيص صلاحيات مشرف:**
   - انقر على زر "الصلاحيات" بجانب التعيين المطلوب
   - قم بتحديد/إلغاء تحديد الصلاحيات المطلوبة
   - انقر على "حفظ الصلاحيات"

3. **تعطيل مشرف:**
   - انقر على زر "تعطيل" بجانب التعيين المطلوب
   - سيتم منع المشرف من الوصول فوراً

4. **تفعيل مشرف:**
   - انقر على زر "تفعيل" بجانب التعيين المعطل
   - سيتمكن المشرف من الوصول مرة أخرى

### للمشرف:

1. **الوصول إلى الهاكاثون:**
   ```
   /supervisor/hackathons/[id]
   ```

2. **إدارة المشاركين:**
   ```
   /supervisor/hackathons/[id]/participants
   ```
   - عرض جميع المشاركين
   - قبول/رفض المشاركين (حسب الصلاحيات)

3. **إدارة الفرق:**
   ```
   /supervisor/hackathons/[id]/teams
   ```
   - عرض جميع الفرق مع أعضائهم
   - **نقل الأعضاء بين الفرق عبر Drag & Drop** (حسب الصلاحيات)
   - إزالة الأعضاء من الفرق بنقرة واحدة (حسب الصلاحيات)
   - واجهة تفاعلية مع مؤشرات بصرية
   - تنبيه توضيحي: "💡 يمكنك سحب الأعضاء وإفلاتهم بين الفرق لنقلهم"

## التحقق من الصلاحيات

يتم التحقق من الصلاحيات على مستويين:

### 1. مستوى API
كل API endpoint يتحقق من:
- هل المشرف مفعل (`isActive = true`)
- هل لديه الصلاحية المطلوبة للعملية

### 2. مستوى الواجهة
الأزرار والخيارات تظهر/تختفي حسب صلاحيات المشرف

## الصلاحيات الافتراضية

عند إنشاء تعيين جديد، يحصل المشرف على جميع الصلاحيات افتراضياً:
```json
{
  "canManageParticipants": true,
  "canApproveParticipants": true,
  "canRejectParticipants": true,
  "canManageTeams": true,
  "canMoveMembers": true,
  "canRemoveMembers": true,
  "canViewReports": true,
  "canExportData": true,
  "canSendMessages": true
}
```

## رسائل الخطأ

عند محاولة الوصول بدون صلاحية:

- **مشرف معطل:** "تم تعطيل صلاحيتك لهذا الهاكاثون من قبل الإدارة"
- **بدون صلاحية إدارة المشاركين:** "ليس لديك صلاحية إدارة المشاركين"
- **بدون صلاحية قبول المشاركين:** "ليس لديك صلاحية قبول المشاركين"
- **بدون صلاحية رفض المشاركين:** "ليس لديك صلاحية رفض المشاركين"
- **بدون صلاحية إدارة الفرق:** "ليس لديك صلاحية إدارة الفرق"

## الملفات المعدلة

### ملفات جديدة:
1. `app/api/admin/supervisor-assignments/[id]/permissions/route.ts`
2. `app/api/admin/supervisor-assignments/[id]/status/route.ts`
3. `app/api/supervisor/hackathons/[id]/teams/[teamId]/members/[participantId]/move/route.ts`
4. `app/api/supervisor/hackathons/[id]/teams/[teamId]/members/[participantId]/route.ts`

### ملفات معدلة:
1. `app/admin/supervisors-management/page.tsx` - إضافة واجهة إدارة الصلاحيات
2. `app/api/supervisor/participants/[id]/status/route.ts` - إضافة التحقق من الصلاحيات
3. `app/api/supervisor/hackathons/[id]/teams/route.ts` - إضافة التحقق من الصلاحيات
4. `app/api/supervisor/hackathons/[id]/settings/route.ts` - تحديث نظام الصلاحيات
5. `app/supervisor/hackathons/[id]/teams/page.tsx` - **إضافة Drag & Drop للأعضاء**

### المكتبات المستخدمة:
- `@dnd-kit/core` - نظام Drag & Drop الأساسي
- `@dnd-kit/sortable` - دعم الترتيب والسحب
- `@dnd-kit/utilities` - أدوات مساعدة للتحويلات

## ملاحظات مهمة

1. **الأدمن لديه صلاحيات كاملة دائماً** - لا يتأثر بنظام الصلاحيات
2. **المشرف المعطل لا يمكنه الوصول لأي شيء** - حتى لو كانت لديه صلاحيات
3. **الصلاحيات تطبق فقط على الهاكاثون المخصص** - المشرف العام لديه صلاحيات على جميع الهاكاثونات
4. **التغييرات فورية** - عند تعديل الصلاحيات، تطبق مباشرة دون الحاجة لإعادة تسجيل الدخول

## الأمان

- ✅ جميع API endpoints محمية بالتحقق من الصلاحيات
- ✅ التحقق من الصلاحيات يتم على مستوى الخادم
- ✅ لا يمكن تجاوز الصلاحيات من جانب العميل
- ✅ سجلات التدقيق تسجل جميع التغييرات في الصلاحيات

## كيفية استخدام Drag & Drop

### للمشرف:
1. افتح صفحة الفرق: `/supervisor/hackathons/[id]/teams`
2. ستجد تنبيه في أعلى الصفحة: "💡 يمكنك سحب الأعضاء وإفلاتهم بين الفرق لنقلهم"
3. اضغط مع الاستمرار على أي عضو (ستظهر أيقونة ⋮⋮)
4. اسحب العضو إلى الفريق المطلوب
5. أفلت العضو - سيتم نقله تلقائياً
6. ستظهر رسالة نجاح: "تم نقل العضو بنجاح"

### ميزات إضافية:
- **زر الحذف**: بجانب كل عضو يوجد زر 🗑️ لإزالته من الفريق
- **مؤشرات بصرية**: العضو يصبح شفافاً أثناء السحب
- **تأكيد الحذف**: عند الضغط على زر الحذف، يظهر تأكيد قبل الإزالة

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

