# 🔄 ميزة إعادة تعيين القوالب

## ✅ الميزة الجديدة

تم إضافة ميزة **إعادة تعيين القوالب** للسماح للمشرفين بإعادة أي قالب (أو جميع القوالب) إلى الوضع الافتراضي في حالة حدوث أخطاء أو تعديلات غير مرغوبة.

---

## 🎯 الأزرار المضافة

### 1. زر "إعادة للوضع الافتراضي" (لقالب واحد)

**الموقع:** في صفحة تحرير القالب، بجانب أزرار "معاينة" و "إرسال تجريبي" و "حفظ"

**الوظيفة:**
- إعادة القالب المحدد فقط إلى الوضع الافتراضي
- يحذف جميع التعديلات على هذا القالب
- يطلب تأكيد من المستخدم قبل التنفيذ

**التصميم:**
- لون برتقالي (`border-orange-200 text-orange-600`)
- أيقونة: `RotateCcw` (سهم دائري)
- النص: "إعادة للوضع الافتراضي"

**الكود:**
```tsx
<Button
  variant="outline"
  size="sm"
  onClick={() => resetSingleTemplate(selectedTemplate.templateKey)}
  className="border-orange-200 text-orange-600 hover:bg-orange-50"
>
  <RotateCcw className="w-4 h-4 ml-2" />
  إعادة للوضع الافتراضي
</Button>
```

---

### 2. زر "إعادة تعيين جميع القوالب" (لكل القوالب)

**الموقع:** في شريط البحث العلوي، بجانب زر "إعادة تحميل القوالب"

**الوظيفة:**
- إعادة **جميع** القوالب إلى الوضع الافتراضي
- يحذف جميع التعديلات على كل القوالب
- يطلب تأكيدين من المستخدم للأمان (تأكيد مزدوج)

**التصميم:**
- لون أحمر (`border-red-200 text-red-600`)
- أيقونة: `Trash2` (سلة مهملات)
- النص: "إعادة تعيين جميع القوالب"

**الكود:**
```tsx
<Button
  onClick={() => resetAllTemplates()}
  variant="outline"
  className="border-red-200 text-red-600 hover:bg-red-50"
>
  <Trash2 className="w-4 h-4 ml-2" />
  إعادة تعيين جميع القوالب
</Button>
```

---

## 🔧 كيف تعمل الميزة؟

### إعادة تعيين قالب واحد

#### 1. المستخدم يضغط الزر
```typescript
onClick={() => resetSingleTemplate(selectedTemplate.templateKey)}
```

#### 2. طلب تأكيد
```typescript
const confirmed = window.confirm(
  'هل أنت متأكد من إعادة تعيين هذا القالب للوضع الافتراضي؟\n\nسيتم فقدان جميع التعديلات على هذا القالب.'
)
```

#### 3. إرسال طلب للـ API
```typescript
const response = await fetch('/api/admin/email-templates/reset', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ templateKey })
})
```

#### 4. الـ API يحذف القالب القديم ويُنشئ واحد جديد
```typescript
await prisma.emailTemplate.deleteMany({
  where: { templateKey }
})

const template = await prisma.emailTemplate.create({
  data: defaultTemplate
})
```

#### 5. تحديث الواجهة
```typescript
await loadTemplates()
setSelectedTemplate(updatedTemplate)
```

---

### إعادة تعيين جميع القوالب

#### 1. المستخدم يضغط الزر
```typescript
onClick={() => resetAllTemplates()}
```

#### 2. تأكيد مزدوج للأمان
```typescript
// التأكيد الأول
const confirmed1 = window.confirm(
  '⚠️ تحذير: هل أنت متأكد من إعادة تعيين جميع القوالب؟'
)

// التأكيد الثاني
const confirmed2 = window.confirm(
  '⚠️ تأكيد نهائي: سيتم فقدان جميع التعديلات على كل القوالب!\n\nهل تريد المتابعة؟'
)
```

#### 3. استخدام نفس API التهيئة
```typescript
const response = await fetch('/api/admin/email-templates/initialize', {
  method: 'POST',
  credentials: 'include'
})
```

#### 4. تحديث الواجهة
```typescript
setTemplates(data.templates || [])
setSelectedTemplate(null)
```

---

## 📋 API Endpoint الجديد

### `/api/admin/email-templates/reset`

**Method:** `POST`

**Body:**
```json
{
  "templateKey": "rejection"
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "تم إعادة تعيين القالب بنجاح",
  "template": {
    "id": "...",
    "templateKey": "rejection",
    "nameAr": "رفض المشاركة",
    "subject": "...",
    "bodyHtml": "...",
    ...
  }
}
```

**Response (Error):**
```json
{
  "error": "القالب المطلوب غير موجود في القوالب الافتراضية"
}
```

**الصلاحيات:**
- يسمح لـ `admin` و `supervisor`

---

## 🎨 تجربة المستخدم

### سيناريو 1: المشرف بوّظ قالب واحد

1. المشرف يفتح قالب "رفض المشاركة"
2. يعدّل المحتوى ويحفظ
3. يكتشف أنه أخطأ في التعديل
4. يضغط على زر **"إعادة للوضع الافتراضي"** (البرتقالي)
5. يظهر تأكيد: "هل أنت متأكد...؟"
6. يضغط "OK"
7. ✅ يتم إعادة القالب للوضع الافتراضي
8. يظهر إشعار: "تم إعادة القالب للوضع الافتراضي بنجاح"

---

### سيناريو 2: المشرف بوّظ كل القوالب

1. المشرف عدّل عدة قوالب بشكل خاطئ
2. يريد البدء من جديد
3. يضغط على زر **"إعادة تعيين جميع القوالب"** (الأحمر)
4. يظهر تأكيد أول: "⚠️ تحذير: هل أنت متأكد...؟"
5. يضغط "OK"
6. يظهر تأكيد ثاني: "⚠️ تأكيد نهائي: سيتم فقدان جميع التعديلات...؟"
7. يضغط "OK"
8. ✅ يتم إعادة جميع القوالب للوضع الافتراضي
9. يظهر إشعار: "تم إعادة تعيين 8 قالب بنجاح"

---

## 🔒 الأمان

### تأكيد واحد لقالب واحد
```javascript
window.confirm('هل أنت متأكد من إعادة تعيين هذا القالب...؟')
```

### تأكيد مزدوج لجميع القوالب
```javascript
// التأكيد الأول
window.confirm('⚠️ تحذير: هل أنت متأكد...؟')

// التأكيد الثاني
window.confirm('⚠️ تأكيد نهائي: سيتم فقدان جميع التعديلات...؟')
```

**لماذا تأكيد مزدوج؟**
- إعادة تعيين جميع القوالب عملية خطيرة
- قد يفقد المشرف ساعات من العمل
- التأكيد المزدوج يمنع الأخطاء غير المقصودة

---

## 📊 القوالب الافتراضية المتاحة

جميع هذه القوالب يمكن إعادة تعيينها:

1. ✅ `registration_confirmation` - تأكيد التسجيل
2. 🎉 `acceptance` - قبول المشاركة
3. 💙 `rejection` - رفض المشاركة
4. 🏆 `team_formation` - تكوين الفريق
5. 👋 `welcome` - ترحيب
6. 🔔 `reminder` - تذكير عام
7. 📊 `evaluation_results` - نتائج التقييم
8. 📜 `certificate_ready` - الشهادة جاهزة

---

## 🎯 حالات الاستخدام

### متى تستخدم "إعادة للوضع الافتراضي" (قالب واحد)؟

✅ **استخدم عندما:**
- عدّلت قالب واحد بشكل خاطئ
- تريد العودة للتصميم الأصلي لقالب معين
- جرّبت تعديلات ولم تعجبك

❌ **لا تستخدم عندما:**
- تريد حفظ بعض التعديلات (استخدم "حفظ" بدلاً من ذلك)
- تريد إعادة جميع القوالب (استخدم الزر الآخر)

---

### متى تستخدم "إعادة تعيين جميع القوالب"؟

✅ **استخدم عندما:**
- عدّلت عدة قوالب بشكل خاطئ
- تريد البدء من الصفر
- حدثت مشاكل كبيرة في التنسيق

❌ **لا تستخدم عندما:**
- تريد إعادة قالب واحد فقط (استخدم الزر الآخر)
- لديك تعديلات مهمة تريد الاحتفاظ بها

---

## 💡 نصائح للمشرفين

### 1. اختبر قبل الحفظ
- استخدم زر "معاينة" قبل الحفظ
- أرسل "إيميل تجريبي" للتأكد

### 2. احفظ نسخة احتياطية
- قبل التعديلات الكبيرة، انسخ المحتوى في ملف نصي
- يمكنك الرجوع إليه إذا احتجت

### 3. استخدم المحرر البسيط
- المحرر البسيط أقل عرضة للأخطاء
- استخدم المحرر المتقدم فقط إذا كنت تعرف HTML

### 4. لا تخف من التجربة
- يمكنك دائماً إعادة التعيين
- جرّب تعديلات مختلفة حتى تجد الأفضل

---

## 🎉 الفوائد

### للمشرفين
- ✅ راحة البال (يمكن التراجع عن الأخطاء)
- ✅ حرية التجربة (بدون خوف)
- ✅ توفير الوقت (بدلاً من التعديل اليدوي)

### للنظام
- ✅ استقرار أكبر (يمكن إصلاح المشاكل بسرعة)
- ✅ تجربة مستخدم أفضل
- ✅ أقل طلبات دعم

---

## 📁 الملفات المعدلة/المضافة

### ملفات معدلة:
1. ✅ `app/supervisor/email-management/page.tsx`
   - إضافة دالة `resetSingleTemplate()`
   - إضافة دالة `resetAllTemplates()`
   - إضافة زر "إعادة للوضع الافتراضي"
   - إضافة زر "إعادة تعيين جميع القوالب"
   - إضافة أيقونات `RotateCcw` و `Trash2`

### ملفات جديدة:
1. ✅ `app/api/admin/email-templates/reset/route.ts`
   - API endpoint لإعادة تعيين قالب واحد
   - يحتوي على جميع القوالب الافتراضية
   - يدعم صلاحيات admin و supervisor

---

## 🚀 كيفية الاستخدام

### إعادة تعيين قالب واحد:

1. افتح صفحة إدارة الإيميلات
2. اختر القالب الذي تريد إعادة تعيينه
3. اضغط على زر **"إعادة للوضع الافتراضي"** (البرتقالي)
4. اضغط "OK" في رسالة التأكيد
5. ✅ تم! القالب عاد للوضع الافتراضي

### إعادة تعيين جميع القوالب:

1. افتح صفحة إدارة الإيميلات
2. اضغط على زر **"إعادة تعيين جميع القوالب"** (الأحمر) في الأعلى
3. اضغط "OK" في التأكيد الأول
4. اضغط "OK" في التأكيد الثاني
5. ✅ تم! جميع القوالب عادت للوضع الافتراضي

---

## ✅ تم بنجاح!

**الآن المشرفون يمكنهم:**
- ✅ تجربة تعديلات مختلفة بدون خوف
- ✅ إعادة أي قالب للوضع الافتراضي بضغطة زر
- ✅ إعادة جميع القوالب دفعة واحدة إذا لزم الأمر
- ✅ العمل بثقة أكبر

**تجربة مستخدم ممتازة! 🎊**

