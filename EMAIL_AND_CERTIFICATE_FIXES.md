# ✅ إصلاح مشكلة الإيميل وإنشاء صفحة إدارة الشهادات

## 🎉 ملخص التنفيذ

تم إصلاح مشكلتين رئيسيتين وإضافة ميزة جديدة:

1. **إصلاح مشكلة عدم ظهور التعديلات في الإيميل التجريبي** ✅
2. **إنشاء صفحة شاملة لإدارة الشهادات للمشرف** 🚧 (قيد الإنشاء)

---

## 1. إصلاح مشكلة الإيميل التجريبي ✅

### **المشكلة:**
عند تعديل قالب الإيميل والضغط على "إرسال تجريبي"، كانت التعديلات لا تظهر في الإيميل المرسل.

### **السبب:**
- المستخدم يعدل القالب في الواجهة (state)
- لكن لا يضغط "حفظ" قبل إرسال الإيميل التجريبي
- API الإرسال التجريبي يقرأ من **قاعدة البيانات** وليس من الـ state
- لذلك يرسل النسخة القديمة المحفوظة

### **الحل المطبق:**

#### **A. حفظ تلقائي (Auto-Save)**
```typescript
// حفظ تلقائي بعد 2 ثانية من التوقف عن الكتابة
const autoSaveTemplate = (template: EmailTemplate) => {
  if (autoSaveTimeout) {
    clearTimeout(autoSaveTimeout)
  }

  const timeout = setTimeout(() => {
    console.log('🔄 Auto-saving template...')
    saveTemplate(template, true) // حفظ صامت
  }, 2000)

  setAutoSaveTimeout(timeout)
}
```

#### **B. تنبيه عند وجود تعديلات غير محفوظة**
```typescript
// تحذير إذا كانت هناك تعديلات غير محفوظة
if (hasUnsavedChanges) {
  const confirmed = window.confirm(
    '⚠️ لديك تعديلات غير محفوظة!\n\n' +
    'سيتم إرسال الإيميل التجريبي بالنسخة المحفوظة في قاعدة البيانات.\n\n' +
    'هل تريد حفظ التعديلات أولاً؟'
  )
  
  if (confirmed) {
    await saveTemplate(template)
    await new Promise(resolve => setTimeout(resolve, 500))
  }
}
```

#### **C. مؤشر بصري للتعديلات غير المحفوظة**
```tsx
<Button className="relative">
  <Save className="w-4 h-4 ml-2" />
  {saving ? 'جاري الحفظ...' : hasUnsavedChanges ? 'حفظ التعديلات' : 'حفظ'}
  {hasUnsavedChanges && !saving && (
    <span className="absolute -top-1 -right-1 w-3 h-3 bg-yellow-400 rounded-full animate-pulse"></span>
  )}
</Button>
```

#### **D. إضافة `credentials: 'include'` للـ API**
```typescript
const response = await fetch('/api/admin/email-templates', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  credentials: 'include', // ✅ إضافة
  body: JSON.stringify(template)
})
```

### **النتيجة:**
- ✅ **حفظ تلقائي** بعد 2 ثانية من التوقف عن الكتابة
- ✅ **تنبيه** إذا حاول المستخدم إرسال إيميل تجريبي بدون حفظ
- ✅ **مؤشر بصري** (نقطة صفراء) عند وجود تعديلات غير محفوظة
- ✅ **خيار حفظ سريع** قبل الإرسال التجريبي

---

## 2. صفحة إدارة الشهادات للمشرف 🚧

### **الهدف:**
إنشاء صفحة شاملة للمشرف لإدارة الشهادات بطريقة احترافية وسهلة.

### **المميزات المخططة:**

#### **A. اختيار الهاكاثون**
- قائمة منسدلة لاختيار الهاكاثون
- عرض معلومات الهاكاثون المحدد

#### **B. إعدادات الشهادة**
- رفع قالب الشهادة على Cloudinary
- ضبط موضع الاسم (اضغط على الشهادة)
- تغيير لون الاسم
- معاينة مباشرة مع اسم تجريبي
- حفظ الإعدادات

#### **C. إدارة شهادات المشاركين**
- عرض قائمة المشاركين المقبولين
- رفع شهادة لكل مشارك
- إرسال الشهادات عبر البريد الإلكتروني
- إرسال جماعي لجميع المشاركين
- تتبع من رفع ومن لم يرفع
- تتبع من تم إرساله ومن لم يُرسل

#### **D. إدارة شهادات المحكمين**
- عرض قائمة المحكمين
- رفع شهادة لكل محكم
- إرسال الشهادات
- إرسال جماعي
- تتبع الحالة

#### **E. إدارة شهادات المشرفين**
- عرض قائمة المشرفين الآخرين
- رفع شهادة لكل مشرف
- إرسال الشهادات
- إرسال جماعي
- تتبع الحالة

### **الحالة الحالية:**
- ✅ تم إنشاء الهيكل الأساسي للصفحة
- ✅ تم إضافة اختيار الهاكاثون
- ✅ تم إضافة معاينة الشهادة
- ✅ تم إضافة إعدادات الموضع واللون
- 🚧 **قيد الإنشاء:** رفع قالب الشهادة
- 🚧 **قيد الإنشاء:** إدارة شهادات المشاركين
- 🚧 **قيد الإنشاء:** إدارة شهادات المحكمين
- 🚧 **قيد الإنشاء:** إدارة شهادات المشرفين

---

## 📊 الملفات المعدلة

### **1. app/supervisor/email-management/page.tsx**
**التعديلات:**
- ✅ إضافة state للتعديلات غير المحفوظة (`hasUnsavedChanges`)
- ✅ إضافة state لمؤقت الحفظ التلقائي (`autoSaveTimeout`)
- ✅ تعديل دالة `saveTemplate` لدعم الحفظ الصامت
- ✅ إضافة دالة `autoSaveTemplate` للحفظ التلقائي
- ✅ تعديل `onChange` للحقول لتفعيل الحفظ التلقائي
- ✅ إضافة تنبيه قبل إرسال إيميل تجريبي بدون حفظ
- ✅ إضافة مؤشر بصري للتعديلات غير المحفوظة
- ✅ إضافة `credentials: 'include'` لجميع طلبات API

**الأسطر المعدلة:**
- السطر 78-82: إضافة state جديد
- السطر 197-244: تعديل دالة الحفظ وإضافة الحفظ التلقائي
- السطر 439-475: إضافة تنبيه قبل الإرسال التجريبي
- السطر 835-846: إضافة مؤشر بصري للتعديلات
- السطر 852-865: تفعيل الحفظ التلقائي للعنوان
- السطر 896-911: تفعيل الحفظ التلقائي للمحرر البسيط
- السطر 937-951: تفعيل الحفظ التلقائي للمحرر المتقدم

### **2. app/supervisor/certificate-management/page.tsx** (جديد)
**الحالة:** 🚧 قيد الإنشاء

**ما تم إنجازه:**
- ✅ الهيكل الأساسي للصفحة
- ✅ اختيار الهاكاثون
- ✅ معاينة الشهادة
- ✅ إعدادات الموضع واللون
- ✅ Canvas للرسم والتفاعل

**ما يحتاج إكمال:**
- 🚧 رفع قالب الشهادة
- 🚧 حفظ الإعدادات
- 🚧 تبويب المشاركين
- 🚧 تبويب المحكمين
- 🚧 تبويب المشرفين
- 🚧 APIs للرفع والإرسال

---

## 🎯 كيفية الاستخدام

### **إصلاح الإيميل:**

1. **افتح صفحة إدارة الإيميلات:**
   ```
   /supervisor/email-management
   ```

2. **اختر قالب إيميل**

3. **عدّل المحتوى:**
   - سيتم الحفظ تلقائياً بعد 2 ثانية من التوقف عن الكتابة
   - ستظهر نقطة صفراء على زر "حفظ" إذا كانت هناك تعديلات غير محفوظة

4. **أرسل إيميل تجريبي:**
   - أدخل البريد الإلكتروني التجريبي
   - اضغط "إرسال تجريبي"
   - إذا كانت هناك تعديلات غير محفوظة، ستظهر رسالة تحذير
   - اختر "نعم" لحفظ التعديلات أولاً

5. **تحقق من الإيميل:**
   - الآن يجب أن تظهر التعديلات في الإيميل المرسل ✅

---

### **صفحة إدارة الشهادات (قيد الإنشاء):**

1. **افتح الصفحة:**
   ```
   /supervisor/certificate-management
   ```

2. **اختر الهاكاثون**

3. **ضبط إعدادات الشهادة:**
   - اضغط على الشهادة لتحديد موضع الاسم
   - غيّر اللون
   - جرّب اسم معاينة مختلف

4. **رفع قالب مخصص:** (قيد الإنشاء)

5. **إدارة الشهادات:** (قيد الإنشاء)

---

## 🚀 الخطوات التالية

### **لإكمال صفحة إدارة الشهادات:**

1. **إضافة رفع قالب الشهادة:**
   - زر رفع الملف
   - رفع على Cloudinary
   - تحديث المعاينة

2. **إضافة حفظ الإعدادات:**
   - زر حفظ
   - API لحفظ الإعدادات
   - تأكيد الحفظ

3. **إضافة تبويب المشاركين:**
   - قائمة المشاركين المقبولين
   - زر رفع شهادة لكل مشارك
   - زر إرسال شهادة
   - زر إرسال جماعي
   - إحصائيات

4. **إضافة تبويب المحكمين:**
   - قائمة المحكمين
   - نفس المميزات

5. **إضافة تبويب المشرفين:**
   - قائمة المشرفين
   - نفس المميزات

6. **إنشاء APIs المطلوبة:**
   - `/api/supervisor/certificates/upload` - رفع شهادة
   - `/api/supervisor/certificates/send` - إرسال شهادة
   - `/api/supervisor/certificates/send-bulk` - إرسال جماعي

---

## 📝 ملاحظات مهمة

### **للإيميل:**
- ✅ الحفظ التلقائي يعمل بعد 2 ثانية من التوقف عن الكتابة
- ✅ يمكن تعطيل الحفظ التلقائي إذا أردت (بإزالة استدعاء `autoSaveTemplate`)
- ✅ التنبيه يظهر فقط إذا كانت هناك تعديلات غير محفوظة
- ✅ المؤشر البصري (النقطة الصفراء) يختفي بعد الحفظ

### **للشهادات:**
- 🚧 الصفحة قيد الإنشاء - الهيكل الأساسي جاهز
- 🚧 يحتاج إكمال الأقسام المتبقية
- 🚧 يحتاج APIs إضافية

---

## 🎊 **إصلاح الإيميل مكتمل! صفحة الشهادات قيد الإنشاء!**

**هل تريد أن أكمل صفحة إدارة الشهادات الآن؟** 🚀

